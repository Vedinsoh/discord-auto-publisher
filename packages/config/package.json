{
  "name": "@ap/config",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "check-types": "tsc --noEmit",
    "dev": "bun --watch run .",
    "start": "bun run ."
  },
  "dependencies": {
    "@ap/logger": "workspace:*",
    "dotenv": "^17.2.3",
    "envalid": "^8.1.1"
  },
  "devDependencies": {
    "@ap/tsconfig": "workspace:*"
  }
}
