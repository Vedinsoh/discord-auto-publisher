# Auth.js
AUTH_SECRET=                              # openssl rand -base64 32
DISCORD_CLIENT_ID=                        # Discord application client ID
DISCORD_CLIENT_SECRET=                    # Discord application client secret

# Backend (server-only, never exposed to client)
FREE_BACKEND_URL=http://localhost:3101    # Free edition backend
PREMIUM_BACKEND_URL=http://localhost:3102 # Premium edition backend

# Paddle
NEXT_PUBLIC_PADDLE_CLIENT_TOKEN=
NEXT_PUBLIC_PADDLE_ENVIRONMENT=sandbox    # sandbox | production

# Premium bot
NEXT_PUBLIC_PREMIUM_BOT_CLIENT_ID=
