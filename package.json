{
  "name": "discord-auto-publisher",
  "version": "1.1.0",
  "description": "Automatically publish message in announcement channels.",
  "main": "bot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Forcellrus",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^12.2.0",
    "dotenv": "^8.2.0",
    "node-fetch": "^2.6.0"
  }
}
